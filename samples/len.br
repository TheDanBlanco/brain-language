let x = [1,2,3];
let y = len(x);
print("x is of length: ", y);

print("also works inline: ", len(x))

let s = "give me a string length";
print("this should be 23: ", len(s));

let cofc = [[1],[2,3,4],[3,4,5,6]];

for c in cofc {
	print(len(c));
}

let map = {
	"teamOneScore": 5,
	"teamTwoScore": 2,
};

print("length of map: ", len(map));

